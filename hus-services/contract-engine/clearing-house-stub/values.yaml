nodeSelector:
  role: hus

# Stub is internal only — never exposed outside the cluster.
ingress:
  enabled: false

clearingHouseStub:
  environment: "production"
  logLevel: INFO

serviceMonitor:
  enabled: false

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 256Mi